<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
  <h5 class="my-0 mr-md-auto font-weight-normal">
    <%= link_to "eventBrite", events_path, class:"my-0 mr-md-auto font-weight-normal text-decoration-none"%>
  </h5>
  <nav class="my-2 my-md-0 mr-md-3">
  <% if user_signed_in? %>
    <%= link_to "Profil", user_path(current_user), class:"p-2 text-dark"%>
  <% end %>
  </nav>


  <% if user_signed_in? %>
    <%= link_to "Créer un événement", new_event_path, class:"btn btn-primary"%>
  <% end %>

  <% if user_signed_in? %>
    <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class:"badge badge-secondary ml-3"%>
    <% else %>
    <%= link_to "Se connecter", new_user_session_path, class:"btn btn-outline-primary"%>
  <% end %>



</div>